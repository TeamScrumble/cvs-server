spring:
  application:
    name: product-service

server:
  port: 8083
  forward-headers-strategy: framework

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

springdoc:
  api-docs:
    path: /api/product/oas.json
  writer-with-default-pretty-printer: true